use std::io;

fn main() {
    let mut line = String::new();
    let mut result = String::new();
    let number : u32 = io::stdin()
        .read_line(&mut hey)
        .parse()
        .expect("could not read line");

    
    println!("{}",result)
}   